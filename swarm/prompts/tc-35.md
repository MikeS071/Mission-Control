# TC-35: Stream and arena misc route tests

Write unit tests for:
- `src/app/api/tasks/stream/route.ts`
- `src/app/api/arena/reactions/stream/route.ts`
- `src/app/api/arena/progress-summary/route.ts`

Tests go in `src/__tests__/api/streams-arena.test.ts`.

Mock DB. For SSE stream endpoints, test that GET returns proper headers (content-type: text/event-stream). Test:
- GET tasks stream returns SSE headers
- GET arena reactions stream returns SSE headers
- GET progress summary returns data
- Missing auth returns 401

Run `npx jest src/__tests__/api/streams-arena.test.ts --ci` — all tests must pass.

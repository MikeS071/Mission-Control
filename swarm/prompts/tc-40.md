# TC-40: Final test backfill commit

1. Run `npx jest --ci` — verify all tests pass
2. Run `npx jest --coverage --ci` — verify coverage meets target
3. Commit with message: "test: MC test backfill wave 2 — coverage report"
4. Push to dev branch

This is the final verification ticket. Do NOT modify any test files — only verify and commit.

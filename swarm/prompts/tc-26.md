# TC-26: Insights route tests

Write unit tests for `src/app/api/insights/route.ts`.

Tests go in `src/__tests__/api/insights.test.ts`.

Mock DB. Test:
- GET insights list
- POST create insight (with Bearer token auth)
- Missing/invalid auth returns 401
- Required fields validation
- Slug uniqueness handling

Run `npx jest src/__tests__/api/insights.test.ts --ci` — all tests must pass.

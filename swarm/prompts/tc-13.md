# TC-13: Unit tests for telegram API routes

## Scope
Write unit tests for `src/app/api/telegram/`:
- webhook, link-token, disconnect, updates, main route

## Test file
`src/__tests__/api/telegram.test.ts`

## Requirements
- Mock DB pool, auth session, Telegram Bot API calls
- Test: webhook message processing (text, command, callback query), link token generation, disconnect flow, updates polling, invalid webhook payload, signature verification if present
- Min 8 test cases

## Quality gates
1. `npx jest src/__tests__/api/telegram.test.ts` — all pass
2. `npx tsc --noEmit` — no type errors

# TC-22: Admin provision route tests

Write unit tests for:
- `src/app/api/admin/provision/route.ts`
- `src/app/api/admin/provision/list/route.ts`
- `src/app/api/admin/provision/[instanceId]/status/route.ts`

Tests go in `src/__tests__/api/admin-provision.test.ts`.

Mock DB calls, mock admin auth check. Test:
- Provision create/list/status endpoints
- Non-admin returns 403
- Invalid instance ID returns 404
- Valid requests return expected shapes

Run `npx jest src/__tests__/api/admin-provision.test.ts --ci` — all tests must pass.

# TC-39: Integration pass and coverage report

1. Run the full test suite: `npx jest --ci`
2. Fix any failing tests (mock issues, import errors)
3. Run coverage: `npx jest --coverage --ci`
4. Write coverage summary to `TEST-COVERAGE-V2.md`
5. Commit all fixes and the coverage report

Target: 80%+ line coverage. If under 80%, document what's missing.

Run `npx jest --ci` — ALL tests must pass before committing.

# TC-23: Admin provision trial route tests

Write unit tests for:
- `src/app/api/admin/provision/trial/route.ts`
- `src/app/api/admin/provision/trial/[instanceId]/revoke/route.ts`

Tests go in `src/__tests__/api/admin-trial.test.ts`.

Mock DB, mock admin auth. Test:
- Trial creation with valid params
- Trial revocation
- Non-admin rejection
- Invalid/missing params

Run `npx jest src/__tests__/api/admin-trial.test.ts --ci` — all tests must pass.

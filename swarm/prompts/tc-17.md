# TC-17: Unit tests for middleware

## Scope
Write unit tests for `src/middleware.ts`:
- Auth redirects (unauthenticated → /signin)
- Protected route guarding (/dashboard/*, /admin/*)
- Public route passthrough (/signin, /signup, /api/*)
- Tenant scoping if present

## Test file
`src/__tests__/middleware.test.ts`

## Requirements
- Mock `next-auth/jwt` getToken, NextRequest/NextResponse
- Test: unauthenticated user redirected from /dashboard, authenticated user passes through, public routes accessible without auth, admin routes check isAdmin, API routes pass through
- Min 6 test cases

## Quality gates
1. `npx jest src/__tests__/middleware.test.ts` — all pass
2. `npx tsc --noEmit` — no type errors

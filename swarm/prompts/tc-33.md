# TC-33: Webhook and newsletter route tests

Write unit tests for:
- `src/app/api/webhook/github/route.ts`
- `src/app/api/newsletter/unsubscribe/route.ts`

Tests go in `src/__tests__/api/webhook-newsletter.test.ts`.

Mock DB. Test:
- POST GitHub webhook with valid signature
- Invalid/missing signature returns 401
- GET unsubscribe with valid token
- Invalid unsubscribe token

Run `npx jest src/__tests__/api/webhook-newsletter.test.ts --ci` — all tests must pass.

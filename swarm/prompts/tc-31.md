# TC-31: Workspace route tests

Write unit tests for:
- `src/app/api/workspace/file/route.ts`
- `src/app/api/workspace/files/route.ts`

Tests go in `src/__tests__/api/workspace.test.ts`.

Mock filesystem operations and auth. Test:
- GET file contents
- GET file list
- PUT file update
- File not found returns 404
- Missing auth returns 401

Run `npx jest src/__tests__/api/workspace.test.ts --ci` — all tests must pass.

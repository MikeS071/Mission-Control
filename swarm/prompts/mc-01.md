# mc-01: Admin layout + navigation scaffold

Build the admin section layout with sidebar navigation.

Create:
- `src/app/admin/layout.tsx` — admin layout with sidebar nav, auth-gated (redirect if not admin)
- `src/app/admin/page.tsx` — admin dashboard landing (placeholder cards for Users, Tenants, System)
- `src/components/admin/AdminSidebar.tsx` — sidebar with links: Dashboard, Users, Tenants, System

Admin check: user session must have `tenantId === 1` (tenant 1 = platform admin).

Reference `src/app/layout.tsx` for the existing layout pattern and `src/app/dashboard/` for page structure.

Use existing auth: `import { getServerSession } from 'next-auth'` and `authOptions` from `src/lib/auth.ts`.

Tailwind dark theme. Keep it minimal — real content comes in later tickets.

Run: `npx tsc --noEmit` (no type errors in new files)
Run: `npx jest --ci` (existing tests still pass)
Commit when done.

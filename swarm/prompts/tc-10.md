# TC-10: Unit tests for billing API routes

## Scope
Write unit tests for billing API routes under `src/app/api/billing/`:
- List the routes by reading the directory
- Cover: checkout session creation, portal redirect, Stripe webhook handling, usage tracking

## Test file
`src/__tests__/api/billing.test.ts`

## Requirements
- Mock Stripe SDK, DB pool, auth session
- Test: create checkout session, portal session, webhook signature verification (valid + invalid), subscription state update, usage query
- Min 8 test cases

## Quality gates
1. `npx jest src/__tests__/api/billing.test.ts` — all pass
2. `npx tsc --noEmit` — no type errors

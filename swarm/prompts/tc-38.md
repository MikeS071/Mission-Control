# TC-38: Feature requests route tests

Write unit tests for `src/app/api/feature-requests/route.ts`.

Tests go in `src/__tests__/api/feature-requests.test.ts`.

Mock DB. Test:
- GET feature requests list
- POST new feature request
- Missing required fields returns 400
- Public endpoint (no auth required)

Run `npx jest src/__tests__/api/feature-requests.test.ts --ci` — all tests must pass.

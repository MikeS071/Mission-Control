# TC-28: Events and heartbeat route tests

Write unit tests for:
- `src/app/api/events/route.ts`
- `src/app/api/heartbeats/route.ts`
- `src/app/api/heartbeat/alert/route.ts`

Tests go in `src/__tests__/api/events-heartbeats.test.ts`.

Mock DB, mock auth. Test:
- GET events list with pagination
- POST/GET heartbeats
- POST heartbeat alert
- Missing tenant returns 401

Run `npx jest src/__tests__/api/events-heartbeats.test.ts --ci` — all tests must pass.

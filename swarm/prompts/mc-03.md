# mc-03: Admin tenants list page

Build the admin tenants list page.

Create:
- `src/app/admin/tenants/page.tsx` — server component, fetches tenants from DB, renders table

Table columns: ID, Name, Slug, Plan, Created, User Count
Use `src/db/schema.ts` → `tenants` table. Count users per tenant with a subquery or join.

Reference `src/app/admin/layout.tsx` (created by mc-01) for the admin layout.
Each row should link to `/admin/tenants/[id]` (built in mc-04).

Tailwind dark theme table.

Run: `npx tsc --noEmit` (no type errors in new files)
Run: `npx jest --ci` (existing tests still pass)
Commit when done.

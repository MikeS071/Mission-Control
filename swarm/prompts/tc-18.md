# TC-18: Test integration pass

## Scope
Run the full test suite and fix any issues:
1. `npx jest --ci` — all suites must pass
2. Fix any cross-test interference (shared state, mock leaks)
3. Fix any import errors or missing mocks
4. Ensure no test modifies global state without cleanup

## Test file
No new test file — fix existing ones

## Requirements
- Run `npx jest --ci --verbose` and capture output
- If any test fails, read the error, fix the test or add missing mocks
- Ensure `afterEach` / `beforeEach` properly reset mocks
- All suites green before committing

## Quality gates
1. `npx jest --ci` — 0 failures
2. `npx tsc --noEmit` — no type errors

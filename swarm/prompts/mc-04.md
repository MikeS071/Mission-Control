# mc-04: Admin tenant detail page

Build the admin tenant detail page.

Create:
- `src/app/admin/tenants/[id]/page.tsx` — server component showing tenant details

Show: name, slug, plan, created date, list of users in this tenant, usage stats if available.
Use `src/db/schema.ts` → `tenants` and `users` tables.

Reference `src/app/admin/tenants/page.tsx` (created by mc-03) for the pattern.

Tailwind dark theme. Read-only in v1 (no edit forms).

Run: `npx tsc --noEmit` (no type errors in new files)
Run: `npx jest --ci` (existing tests still pass)
Commit when done.

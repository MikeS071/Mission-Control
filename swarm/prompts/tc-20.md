# TC-20: Final commit

## Scope
Final quality check and commit:
1. `npx jest --ci` — all pass
2. `npx tsc --noEmit` — no type errors
3. Commit all test files with evidence

## Requirements
- Run full test suite, capture pass count
- Run type check
- If anything fails, fix it
- Commit:
```bash
git add -A
git commit -m "test: MC test backfill — comprehensive unit test coverage

Suites: X passed
Tests: Y passed, 0 failed
Coverage: Z% statements"
git push origin HEAD
```

## Quality gates
1. All tests pass
2. No type errors
3. Committed and pushed

# TC-25: Settings and tenants route tests

Write unit tests for:
- `src/app/api/settings/route.ts`
- `src/app/api/tenants/me/route.ts`

Tests go in `src/__tests__/api/settings-tenants.test.ts`.

Mock DB, mock auth headers. Test:
- GET/PUT settings
- GET current tenant
- Missing auth returns 401
- Invalid settings payload returns 400

Run `npx jest src/__tests__/api/settings-tenants.test.ts --ci` — all tests must pass.

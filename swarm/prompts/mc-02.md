# mc-02: Admin users list page

Build the admin users list page.

Create:
- `src/app/admin/users/page.tsx` — server component, fetches users from DB, renders table

Table columns: ID, Email, Tenant, Created, Last Login
Use `src/db/schema.ts` → `users` table. Join with `tenants` for tenant name.

Reference `src/app/admin/layout.tsx` (created by mc-01) for the admin layout.
Reference `src/db/schema.ts` for the schema.

Tailwind dark theme table. No pagination needed in v1 (just list all).

Run: `npx tsc --noEmit` (no type errors in new files)
Run: `npx jest --ci` (existing tests still pass)
Commit when done.

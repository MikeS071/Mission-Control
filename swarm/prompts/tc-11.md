# TC-11: Unit tests for activity API routes

## Scope
Write unit tests for activity API routes under `src/app/api/activity/`:
- Cover: events listing, SSE stream, drain endpoint, comments, reactions

## Test file
`src/__tests__/api/activity.test.ts`

## Requirements
- Mock DB pool, auth session
- Test: list events (paginated), post comment, toggle reaction, drain endpoint clears queue, SSE headers correct, unauthorized access blocked
- Min 8 test cases

## Quality gates
1. `npx jest src/__tests__/api/activity.test.ts` — all pass
2. `npx tsc --noEmit` — no type errors

# TC-34: Meta and health route tests

Write unit tests for:
- `src/app/api/meta/diag/route.ts`
- `src/app/api/meta/health/route.ts`
- `src/app/api/meta/version/route.ts`
- `src/app/api/health/route.ts`

Tests go in `src/__tests__/api/meta-health.test.ts`.

These are simple endpoints. Test:
- GET health returns 200
- GET version returns version string
- GET diag returns diagnostic info
- No auth required for health/version

Run `npx jest src/__tests__/api/meta-health.test.ts --ci` — all tests must pass.

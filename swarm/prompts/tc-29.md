# TC-29: Gamification route tests

Write unit tests for:
- `src/app/api/gamification/challenges/route.ts`
- `src/app/api/gamification/challenges/[id]/complete/route.ts`

Tests go in `src/__tests__/api/gamification.test.ts`.

Mock DB. Test:
- GET challenges list
- POST complete challenge
- Invalid challenge ID
- Already completed challenge

Run `npx jest src/__tests__/api/gamification.test.ts --ci` — all tests must pass.

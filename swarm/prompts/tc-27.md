# TC-27: Waitlist route tests

Write unit tests for:
- `src/app/api/waitlist/route.ts`
- `src/app/api/waitlist/emails/route.ts`

Tests go in `src/__tests__/api/waitlist.test.ts`.

Mock DB. Test:
- POST add to waitlist
- GET waitlist emails (admin only)
- Duplicate email handling
- Invalid email format

Run `npx jest src/__tests__/api/waitlist.test.ts --ci` — all tests must pass.
